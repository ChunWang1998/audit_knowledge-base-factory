[
  {
    "id": "bridge-receiver-Overlayer-001",
    "source": "HackenPDFTXT/Overlayer.txt",
    "category": "Accounting",
    "severity": "High",
    "severity_level": 3,
    "contract_name": "AaveHandler",
    "functions_affected": [
      "totalSupply",
      "supply"
    ],
    "description": "`OverlayerWrap` 实现了 LayerZero OFT 标准，跨链转账时在源链执行销毁、在目标链执行铸造。每次从枢纽链（Ethereum）跨链转出代币，`totalSupply()` 就会减少对应数量。`AaveHandler::supply()` 以 `totalSupply()` 为上限，通过 `normalizedSupply - totalSuppliedCollateral` 计算可供给 Aave 的额度。当跨链转账使 `totalSupply()` 降至 `totalSuppliedCollateral` 以下时，该差值下溢，导致所有后续 `supply()` 调用回滚，彻底阻断枢纽链上的收益生成。",
    "impact": "跨链 OFT 转账后，`AaveHandler::supply()` 因算术下溢而回滚，造成所有 Aave 供给操作的拒绝服务，协议无法对新存入的抵押品产生收益，相关集成流程全部中断。",
    "code_snippet": "function totalSupply(...) { ... }  // see audit report",
    "location": "AaveHandler.sol (totalSupply, supply)",
    "recommendation": "修改 `AaveHandler::supply()` 中 `totalSuppliedCollateral` 的差值计算逻辑，单独追踪已跨链转出的总量 `totalBridgedOut`，使用 `totalSupply() + totalBridgedOut` 作为有效的全局供给上限，防止跨链供给变化导致算术下溢。",
    "tags": [
      "F-2026-15250",
      "overlayer-wrap",
      "oft-burn-mint",
      "aave-handler-supply",
      "hub-totalsupply"
    ],
    "patterns": [
      "OFT bridge burn reduces OverlayerWrap totalSupply on hub",
      "AaveHandler supply cap uses totalSupply - totalSuppliedCollateral",
      "bridged-out supply causes underflow and supply() DoS"
    ]
  },
  {
    "id": "bridge-receiver-manager-001",
    "source": "cyfrin/manager.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "YieldManager",
    "functions_affected": [
      "reportYield"
    ],
    "description": "`YieldManager` 的结算架构旨在优先使用质押 ETH 产生的收益偿还 `lstLiabilities` 和运营义务，然后才向 Linea L2 报告正收益。但结算逻辑受限于 `stVault` 的可用余额。当所有 ETH 均被质押时，金库可用余额为零，即便产生了正收益，负债偿还也会被推迟，全额收益被直接上报 L2，负债持续累积，形成越来越大的结算缺口。",
    "impact": "系统内负收益持续积累，`lstLiabilities` 上的利息复利增长，降低实际质押收益；同时导致 `_getTotalSystemBalance` 读数虚高、准备金阈值计算错误，加剧多个协议函数的相关会计问题。",
    "code_snippet": "function unknown(...) { ... }  // see audit report",
    "location": "YieldManager.sol (unknown)",
    "recommendation": "在向 L2 报告正收益前，检查所有未结清义务和负债是否可以被结算；仅在扣除全部债务后，才将剩余部分作为可分配收益上报。在代码层面，修改 `reportYield` 使其要求 `totalVaultFunds > lastUserFunds + ALL_OBLIGATIONS` 才将盈余视为可分配收益。",
    "tags": [
      "report-yield",
      "lst-liabilities",
      "stvault-liquidity",
      "yield-manager",
      "linea-settlement"
    ],
    "patterns": [
      "liability repayment needs liquid stVault balance",
      "fully staked vault reports full yield to L2 skipping debt paydown",
      "lstLiabilities compound and system balance overstated"
    ]
  },
  {
    "id": "bridge-receiver-Dexalot-001",
    "source": "HackenPDFTXT/Dexalot.txt",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "PortfolioSub",
    "functions_affected": [
      "depositTokenFromContract",
      "depositToken"
    ],
    "description": "`depositTokenFromContract` 供受信合约代替用户存款，但在 v2.6.3 升级后，存款流程要求通过 `msg.value` 支付原生桥接费用。该函数未标记为 `payable`，也不向内部 `depositToken` 转发任何 ETH，导致到达桥接费用检查时 `msg.value` 始终为零，在所有需要费用的桥接提供商场景下交易均会失败。",
    "impact": "当默认桥接提供商启用费用后，所有通过 `depositTokenFromContract` 操作的受信合约集成将持续失败，造成拒绝服务，而直接调用 `depositToken` 仍可正常支付费用，形成不一致的 API 接口。",
    "code_snippet": "function depositTokenFromContract(...) external { depositToken(...); } // not payable",
    "location": "PortfolioSub.sol (depositTokenFromContract)",
    "recommendation": "将 `depositTokenFromContract` 标记为 `payable`，并将 `msg.value` 转发至内部 `depositToken` 调用，允许受信合约在存款交易中附带所需的原生桥接费用。",
    "tags": [
      "F-2026-14991",
      "deposit-token-from-contract",
      "bridge-fee-msg-value",
      "portfolio-sub",
      "dexalot"
    ],
    "patterns": [
      "v2.6.3 deposit requires msg.value for bridge fee",
      "depositTokenFromContract not payable and forwards no ETH",
      "trusted contract deposits revert when bridge fees enabled"
    ]
  }
]
