[
  {
    "id": "maxwithdraw-tranche-YieldFi-001",
    "source": "sherlockPDFTXT/YieldFi.txt",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Manager",
    "functions_affected": [
      "deposit",
      "_getAndValidateNav"
    ],
    "description": "`Manager` 合约使用线性归属中的 `currentNav`（当前 NAV，慢慢接近目标 `endNav`）作为铸造份额的价格。当一笔大额子弹还款导致 `endNav` 突然跳升时，`currentNav` 仍处于旧的低值。攻击者监控链上事件，在 NAV 归属完成前按低价存款，以低于公允价值的价格获取份额，待 NAV 完成归属后套利。",
    "impact": "攻击者以折扣价铸造过多份额，稀释现有存款人的质押比例，捕获本属于长期持有者的收益。",
    "code_snippet": "shares minted using lagging currentNav not endNav after bullet repayment",
    "location": "Manager.sol (deposit, NAV vesting)",
    "recommendation": "当 NAV 处于上升状态时，使用目标 NAV（`endNav`）而非滞后的 `currentNav` 计算铸造份额，防止攻击者在归属间隙以低于公允价值获取份额。",
    "tags": [
      "manager",
      "current-nav-vesting",
      "end-nav-arbitrage",
      "share-mint-discount",
      "yieldfi"
    ],
    "patterns": [
      "deposit pricing uses linearly vested currentNav",
      "endNav jumps on bullet repayment while currentNav lags",
      "depositor mints excess shares before vesting catches up"
    ]
  },
  {
    "id": "maxwithdraw-tranche-pr50-001",
    "source": "cyfrin/pr50.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "AccountableYield",
    "functions_affected": [
      "_navIsStale",
      "accrueAndProcess"
    ],
    "description": "`AccountableYield` 在 `onRequestRedeem` 中通过 `_navIsStale()` 检查来禁用 NAV 过期时的即时赎回。然而，处理提款队列的 `accrueAndProcess()` 函数是公开可调用的，且没有过期检查守卫。用户可以在提交赎回请求后立即调用 `accrueAndProcess()`，绕过保护机制在 NAV 过期时立即结算提款。",
    "impact": "NAV 过期保护被轻易绕过，用户可以在 NAV 真实值可能已大幅下降的情况下按过期价格提款，从协议中提取不当价值。",
    "code_snippet": "function _navIsStale(...) { ... }  // see audit report",
    "location": "AccountableYield.sol (_navIsStale, accrueAndProcess)",
    "recommendation": "为 `accrueAndProcess()` 及所有可触发 `_processAvailableWithdrawals()` 的公开入口添加 `whenNotStale` 修饰符，确保队列处理始终在 NAV 有效时进行。",
    "tags": [
      "accrue-and-process",
      "nav-stale-bypass",
      "accountable-yield",
      "instant-redeem",
      "withdrawal-queue"
    ],
    "patterns": [
      "onRequestRedeem blocked when _navIsStale",
      "public accrueAndProcess has no stale guard",
      "user queues then calls accrueAndProcess to exit at stale NAV"
    ]
  },
  {
    "id": "maxwithdraw-tranche-accountable-001",
    "source": "cyfrin/accountable.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "AccountableAsyncRedeemVault",
    "functions_affected": [
      "withdraw",
      "maxWithdraw"
    ],
    "description": "`AccountableAsyncRedeemVault::withdraw` 检查 `maxWithdraw(receiver)` 而非正确的 `maxWithdraw(owner/controller)`。EIP-4626 规范要求超过 `maxWithdraw(owner)` 的提款必须回滚，但实现中验证的是接收方的上限，导致所有者可能绕过其份额限制，或接收方的限制被错误地应用于所有者的操作。",
    "impact": "所有者可以绕过其合法提款上限（若接收方的上限更高），同时接收方的提款容量可能因此被错误限制，对 `withdraw()` 造成 DoS。",
    "code_snippet": "// `AccountableAsyncRedeemVault::withdraw` enforces an upper bound on the withdrawal amount by checking `if (assets > maxWi",
    "location": "AccountableAsyncRedeemVault.sol (withdraw, maxWithdraw)",
    "recommendation": "将检查改为 `assets > maxWithdraw(controller)`，使用控制者（所有者）的上限进行验证。",
    "tags": [
      "max-withdraw-owner",
      "withdraw-receiver-check",
      "accountable-async-redeem-vault",
      "eip-4626",
      "withdraw-cap-bypass"
    ],
    "patterns": [
      "withdraw checks maxWithdraw(receiver) not maxWithdraw(owner)",
      "owner can exceed own cap if receiver limit higher",
      "receiver withdrawals incorrectly constrained"
    ]
  },
  {
    "id": "maxwithdraw-tranche-cooldown-001",
    "source": "cyfrin/cooldown.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Tranche",
    "functions_affected": [
      "burnSharesAsFee"
    ],
    "description": "`Tranche::burnSharesAsFee` 不验证燃烧后剩余份额是否低于 `MIN_SHARES` 阈值。攻击者可以先存入 1 份份额，通过此函数全部销毁，此时 `totalSupply == 0` 但 `Tranche_NAV > 0`（因 `retentionBps`）。攻击者再铸造 1 wei 份额，设置极端兑换率。合法用户随后的大额存款因兑换率极高只能获得极少量份额（低于 `MIN_SHARES`），导致提款因 `MinSharesViolation` 回滚，资金被永久锁定。",
    "impact": "攻击者通过抢先操控兑换率，可以永久锁定其他用户的存款资金。",
    "code_snippet": "// `Tranche::burnSharesAsFee` is a function that allows burning shares as a fee collection mechanism, distributing the corr",
    "location": "Tranche.sol (burnSharesAsFee)",
    "recommendation": "在 `Tranche::burnSharesAsFee` 末尾调用 `_onAfterWithdrawalChecks`，确保燃烧费用后份额数量不低于 `MIN_SHARES` 安全阈值。",
    "tags": [
      "burn-shares-as-fee",
      "min-shares",
      "tranche-nav-retention",
      "exchange-rate-grief",
      "strata-tranches"
    ],
    "patterns": [
      "burnSharesAsFee can reduce supply below MIN_SHARES",
      "attacker leaves totalSupply 0 with positive Tranche_NAV",
      "remint 1 wei share then large deposits trapped by MinSharesViolation"
    ]
  },
  {
    "id": "maxwithdraw-tranche-cooldown-002",
    "source": "cyfrin/cooldown.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Tranche",
    "functions_affected": [
      "maxWithdraw"
    ],
    "description": "`Tranche::maxWithdraw` 通过 `previewRedeem` 计算可提款资产，而 `previewRedeem` 在查询退出费率时始终传递 `address(0)` 而非实际所有者地址。当所有者为 `SharesCooldown` 时，CDO 明确豁免其退出费，但 `address(0)` 的查询不享有此豁免，导致 `maxWithdraw(address(sharesCooldown))` 返回的值低于 `SharesCooldown` 实际可以提取的金额。",
    "impact": "链下集成和前端查询 `maxWithdraw` 时会获得低于实际的可提款金额，导致有效提款请求被错误拒绝或 UI 显示余额不准确。",
    "code_snippet": "// `Tranche::maxWithdraw(owner)` computes the net assets available to withdraw by calling `previewRedeem(sharesGross)`. The",
    "location": "Tranche.sol (maxWithdraw)",
    "recommendation": "将 `maxWithdraw` 中的 `calculateExitMode` 调用改为传递实际 `owner` 地址，使退出费计算能正确反映 `SharesCooldown` 的费用豁免。",
    "tags": [
      "max-withdraw",
      "preview-redeem",
      "exit-fee-exemption",
      "shares-cooldown",
      "strata-tranches"
    ],
    "patterns": [
      "previewRedeem passes address(0) to calculateExitMode",
      "SharesCooldown exit fee waiver ignored in maxWithdraw",
      "maxWithdraw under-reports withdrawable assets"
    ]
  },
  {
    "id": "maxwithdraw-tranche-tranches-001",
    "source": "cyfrin/tranches.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Accounting",
    "functions_affected": [
      "updateIndexes"
    ],
    "description": "CDO NAV 分配逻辑在 Senior 目标收益为负时，将 Senior 损失转移给 Junior 作为收益。代码正确地递减了 `srtNavT0`，但错误地递增了 `jrtNavT0`（初始 Junior NAV）而非 `jrtNavT1`（最终 Junior NAV）。由于 `jrtNavT1` 在该分支执行前已计算完毕，Senior 损失未被反映到最终 NAV，导致后续的 NAV 总和验证失败，交易回滚。",
    "impact": "任何触发 Senior 负目标收益的 NAV 更新都会回滚，阻止该纪元的会计结算，可能引发依赖更新后 NAV 的所有下游操作失败。",
    "code_snippet": "jrtNavT0 += srtLoss; // should update jrtNavT1 before InvalidNavSpit check",
    "location": "Accounting.sol (updateIndexes / NAV split)",
    "recommendation": "将 `jrtNavT0 += srtLoss` 改为 `jrtNavT1 += srtLoss`，确保 Senior 损失被传播到参与不变量检验的最终 Junior NAV 值。",
    "tags": [
      "negative-senior-target-gain",
      "jrt-nav-t1",
      "invalid-nav-spit",
      "accounting",
      "strata-tranches"
    ],
    "patterns": [
      "senior loss applied to jrtNavT0 after jrtNavT1 computed",
      "jrtNavT1 unchanged so nav sum invariant fails",
      "NAV update reverts InvalidNavSpit on negative senior target"
    ]
  },
  {
    "id": "maxwithdraw-tranche-predeposit-001",
    "source": "cyfrin/predeposit.md",
    "category": "Accounting",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "pUSDeVault",
    "functions_affected": [
      "maxWithdraw"
    ],
    "description": "EIP-4626 规定当金库处于提款会失败的状态（包括暂停时），`maxWithdraw` 必须返回零。`pUSDeVault` 实现了提款暂停机制，但 `maxWithdraw` 未检查暂停状态，在提款暂停时仍返回非零值。集成 `pUSDeVault` 的协议通常先调用 `maxWithdraw` 验证可行性，收到非零值后发起提款却因暂停而回滚，导致集成协议状态错误或流动性分配失误。",
    "impact": "依赖 EIP-4626 合规性进行流动性管理的集成协议，在提款暂停期间会收到错误的 `maxWithdraw` 值，导致其自身操作失败或产生错误状态转换。",
    "code_snippet": "function maxWithdraw(...) { ... }  // see audit report",
    "location": "pUSDeVault.sol (maxWithdraw)",
    "recommendation": "在 `PreDepositVault`（暂停功能所在处）中重写 `maxWithdraw`，当提款暂停时返回零，确保函数始终准确反映实际可提款容量，符合 EIP-4626 规范。",
    "tags": [
      "max-withdraw",
      "withdraw-paused",
      "eip-4626-compliance",
      "pusde-vault",
      "integration-dos"
    ],
    "patterns": [
      "withdraw pause does not zero maxWithdraw",
      "integrators read non-zero maxWithdraw then revert on withdraw",
      "liquidity routers mis-allocate during pause"
    ]
  }
]
