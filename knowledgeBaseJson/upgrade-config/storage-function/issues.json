[
  {
    "id": "storage-function-VechainFoundation-001",
    "source": "HackenPDFTXT/Vechain Foundation.txt",
    "category": "Upgrade / Config",
    "severity": "High",
    "severity_level": 3,
    "contract_name": "Stargate",
    "functions_affected": [
      "unstake",
      "_updatePeriodEffectiveStake"
    ],
    "description": "`Stargate.sol` 中的 `unstake()` 函数在用户取消质押处于 `PENDING` 或 `EXITED` 委托状态的代币时，未能减少验证器的有效质押量。该函数从协议中提取 VET、认领奖励、重置委托映射、销毁代币并返还 VET，但从未调用 `_updatePeriodEffectiveStake` 来减少验证器的已记录质押量。这导致验证器的 `delegatorsEffectiveStake` 在对应代币销毁后仍永久虚高。",
    "impact": "每次此类取消质押操作后，验证器的 `delegatorsEffectiveStake` 持续被高估，破坏奖励与委托核算，使验证器看起来拥有比实际更多的委托质押量。",
    "code_snippet": "function unstake(uint256 tokenId) {\n    // PENDING/EXITED delegation: never calls _updatePeriodEffectiveStake to decrease validator stake\n}",
    "location": "Stargate.sol (unstake)",
    "recommendation": "引入额外的委托状态以区分因出块失败导致的验证器退出与用户主动委托退出，在 `unstake` 函数中对所有非 ACTIVE 委托状态分别调用带有减少标志的 `_updatePeriodEffectiveStake`，然后再重置委托详情。",
    "tags": [
      "effective-stake-inflation",
      "unstake-accounting",
      "PENDING-delegation",
      "validator-stake-phantom"
    ],
    "patterns": [
      "unstake skips effective stake decrease for PENDING",
      "delegatorsEffectiveStake remains inflated after burn"
    ]
  },
  {
    "id": "storage-function-pr50-001",
    "source": "cyfrin/pr50.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "AccountableYield",
    "functions_affected": [
      "Unauthorized",
      "setNavGracePeriod"
    ],
    "description": "`AccountableYield.setNavGracePeriod()` 在 `period < MIN_NAV_GRACE_PERIOD` 时抛出 `Unauthorized()` 错误，但此条件实际上是输入验证失败而非访问控制问题。使用访问控制错误来表示参数验证失败在语义上是错误且具有误导性的。",
    "impact": "误导性错误代码使合法调用方更难诊断 revert 根本原因，也破坏了协议错误分类体系的清晰性。",
    "code_snippet": "function Unauthorized(...) { ... }  // see audit report",
    "location": "AccountableYield.sol (Unauthorized, setNavGracePeriod)",
    "recommendation": "将 `Unauthorized()` revert 替换为专用的输入验证错误，如 `InvalidNavGracePeriod()`，或复用现有的通用无效参数错误，以准确传达 revert 原因。",
    "tags": [
      "wrong-error-type",
      "Unauthorized-for-validation",
      "setNavGracePeriod",
      "misleading-revert"
    ],
    "patterns": [
      "revert Unauthorized() on period < MIN_NAV_GRACE_PERIOD",
      "input validation uses access-control error"
    ]
  },
  {
    "id": "storage-function-Fabstir-001",
    "source": "HackenPDFTXT/Fabstir.txt",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "JobMarketplaceWithModelsUpgradeable",
    "functions_affected": [
      "createSessionJobForModel",
      "createSessionJobForModelWithToken"
    ],
    "description": "当所有者通过 `deactivateModel` 停用模型时，会话创建路径中不存在任何强制措施阻止针对该停用模型创建新会话。会话创建函数通过调用 `nodeRegistry.nodeSupportsModel()` 验证主机是否支持某模型，该函数仅检查主机本地的 `supportedModels` 数组，从不调用 `modelRegistry.isModelApproved(modelId)` 验证模型是否仍处于活跃状态。",
    "impact": "出于安全原因（如权重泄露、有害输出）被所有者停用的模型仍可无限期地被用于创建新会话，使紧急停用机制完全失效，并将租用方暴露在所有者明确认为不安全的模型推理风险中。",
    "code_snippet": "require(nodeRegistry.nodeSupportsModel(host, modelId));\n// never: modelRegistry.isModelApproved(modelId)",
    "location": "JobMarketplaceWithModelsUpgradeable.sol (session creation)",
    "recommendation": "在 `JobMarketplaceWithModelsUpgradeable` 的模型感知会话创建中，通过直接查询 `ModelRegistry` 的 `isModelApproved` 在主机支持验证之前添加检查。",
    "tags": [
      "deactivated-model-bypass",
      "isModelApproved-not-checked",
      "emergency-model-shutdown",
      "session-creation"
    ],
    "patterns": [
      "deactivateModel has no effect on new sessions",
      "nodeSupportsModel ignores global model approval"
    ]
  },
  {
    "id": "storage-function-registry-001",
    "source": "cyfrin/registry.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "AccessControlUpgradeable",
    "functions_affected": [
      "_grantRole",
      "changeAdmin"
    ],
    "description": "`AccessControlUpgradeable::_grantRole` 和 `_revokeRole` 返回布尔值以指示角色是否实际被授予或撤销。`GlobalRegistryService::changeAdmin`、`addOperator` 和 `revokeOperator` 等函数未检查该返回值就直接触发事件，导致即使没有实际角色变更也会发出事件。",
    "impact": "在未发生角色变更时发出误导性事件，可能混淆依赖事件追踪角色状态的链下监控、索引工具和分析系统，导致访问控制追踪错误。",
    "code_snippet": "// `AccessControlUpgradeable::_grantRole` and `_revokeRole` return a `bool` to indicate whether the role was actually grant",
    "location": "AccessControlUpgradeable.sol (_grantRole, changeAdmin)",
    "recommendation": "在 `changeAdmin`、`addOperator` 和 `revokeOperator` 中检查 `_grantRole` 和 `_revokeRole` 的布尔返回值，仅在返回值为 `true` 时才发出相应事件。",
    "tags": [
      "grantRole-return-ignored",
      "misleading-admin-events",
      "GlobalRegistryService",
      "access-control-logging"
    ],
    "patterns": [
      "emit RoleGranted without checking _grantRole bool",
      "duplicate calls emit events with no state change"
    ]
  },
  {
    "id": "storage-function-syntetika-001",
    "source": "cyfrin/syntetika.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "YieldDistributor",
    "functions_affected": [
      "distributeYield"
    ],
    "description": "`YieldDistributed` 事件仅携带收益金额参数，遗漏了 `timestamp` 参数。事件是链下数据分析、历史查询和集成的主要信息来源，缺少时间戳会使调用方需要额外的区块时间戳查询。",
    "impact": "依赖 `YieldDistributed` 事件的链下工具和集成无法直接确定收益分配时间，降低了协议可观测性。",
    "code_snippet": "event YieldDistributed(uint256 amount);  // missing timestamp parameter",
    "location": "Yield distributor (YieldDistributed event)",
    "recommendation": "在 `YieldDistributed` 事件定义中添加 `timestamp` 参数，并在触发时包含 `block.timestamp`。",
    "tags": [
      "incomplete-event-metadata",
      "YieldDistributed",
      "missing-timestamp",
      "off-chain-analytics"
    ],
    "patterns": [
      "YieldDistributed emits amount only",
      "no block.timestamp in event for historical queries"
    ]
  },
  {
    "id": "storage-function-pledge-001",
    "source": "cyfrin/pledge.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Allowlist",
    "functions_affected": [
      "changeUserAccreditation",
      "addReferral",
      "setFundingWallet",
      "changeReferralManager",
      "setPayoutForwardAddress",
      "addToWhitelist"
    ],
    "description": "多个合约中的多个函数在修改存储状态时未发出相应事件，涉及 `Allowlist`、`ReferralManager`、`RemoraIntermediary`、`TokenBank`、`DividendManager`、`RemoraToken`、`PaymentSettler` 等合约的多个配置函数。此外，`UserAllowed` 和 `TokensWithdrawn` 等事件也缺少重要字段。",
    "impact": "缺少存储变更事件使链下观察者无法检测或响应关键配置更改，降低协议透明度，导致监控和集成不可靠。",
    "code_snippet": "// Multiple functions across various contracts modify storage state without emitting corresponding events. Affected functio",
    "location": "Allowlist.sol (changeUserAccreditation, addReferral, setFundingWallet, changeReferralManager)",
    "recommendation": "在所有受影响的函数中添加适当的事件，并在存储状态被修改时发出这些事件。",
    "tags": [
      "missing-storage-events",
      "config-change-opacity",
      "remora-multi-contract",
      "monitoring-gap"
    ],
    "patterns": [
      "Allowlist/TokenBank/DividendManager setters modify storage without events",
      "UserAllowed missing indexed fields"
    ]
  },
  {
    "id": "storage-function-protocol-001",
    "source": "cyfrin/protocol.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SessionManager",
    "functions_affected": [
      "createGame"
    ],
    "description": "`SessionManager::createGame` 中，数组长度一致性检查（`require(_promptStrategies.length == _promptHashes.length, ...)`）在多次存储读取之后才执行，而非在之前。由于存储读取代价高昂，在已付出存储读取开销后才对廉价输入检查失败是浪费的。",
    "impact": "提交不匹配数组长度的用户在交易 revert 前已不必要地支付了存储读取的 gas 费用。",
    "code_snippet": "// In `SessionManager::createGame`, an array-length consistency check (`require(_promptStrategies.length == _promptHashes.l",
    "location": "SessionManager.sol (createGame)",
    "recommendation": "将长度检查移至 `createGame` 顶部，在任何存储读取之前执行，使无效输入能够早期以低成本 revert。",
    "tags": [
      "early-validation",
      "createGame-gas",
      "array-length-before-sload",
      "input-check-order"
    ],
    "patterns": [
      "promptStrategies/promptHashes length check after storage reads",
      "invalid txs pay SLOAD cost before revert"
    ]
  },
  {
    "id": "storage-function-Dexalot-001",
    "source": "HackenPDFTXT/Dexalot.txt",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "PortfolioSub",
    "functions_affected": [
      "bulkTransferTokens",
      "transferToken"
    ],
    "description": "`PortfolioSub` 合约实现了 `autoFillPrivate` 机制，确保用户在 Dexalot L1 上的 Gas Tank 中始终有足够的 ALOT gas 代币。单代币 `transferToken` 函数正确地为接收方调用了 `autoFillPrivate`，但 `bulkTransferTokens` 函数完全省略了这一调用，导致批量转账的接收方在收到代币后 Gas Tank 未被填充。",
    "impact": "批量转账的接收方在收到代币后无法立即在 Dexalot L1 上执行任何交易，破坏了协议的已记录不变量，并在单次和批量转账之间造成行为不一致。",
    "code_snippet": "function bulkTransferTokens(...) {\n    // loops transferToken but omits:\n    // autoFillPrivate(_to, _symbols[0], Tx.AUTOFILL);\n}",
    "location": "PortfolioSub.sol (bulkTransferTokens)",
    "recommendation": "在 `bulkTransferTokens` 末尾添加对 `autoFillPrivate(_to, _symbols[_symbols.length - 1], Tx.AUTOFILL)` 的调用，确保接收方获得与单代币转账路径一致的 gas 自动补充。",
    "tags": [
      "gas-tank-autofill",
      "bulk-transfer-gap",
      "ALOT-gas-refill",
      "inconsistent-transfer-paths"
    ],
    "patterns": [
      "transferToken calls autoFillPrivate for recipient",
      "bulkTransferTokens omits autoFillPrivate"
    ]
  },
  {
    "id": "storage-function-RYT-001",
    "source": "HackenPDFTXT/RYT.txt",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Komiti",
    "functions_affected": [
      "returnFunds",
      "joinGroup"
    ],
    "description": "智能合约提供了 `returnFunds()` 函数，但该函数仅适用于联合贡献者工作流，允许主要贡献者在派息开始前撤销邀请。通过 `joinGroup()` 加入的普通用户在 Komiti 团体未开始、超额填充、停滞或被遗弃时没有任何资金回收机制。",
    "impact": "通过 `joinGroup()` 加入的普通参与者的资金在团体从未开始派息时可能被永久锁定在合约中，即使在明显被遗弃的团体中也无法恢复贡献资金。",
    "code_snippet": "// returnFunds() only for joint contributors; joinGroup users have no refund path if group never starts",
    "location": "Komiti.sol (returnFunds, joinGroup)",
    "recommendation": "为普通用户实现可在派息开始前执行的退款路径，确保退款机制对联合和非联合参与者都有效，并考虑添加管理员紧急退款功能。",
    "tags": [
      "missing-refund-path",
      "joinGroup-locked-funds",
      "rosca-abandoned-group",
      "asymmetric-refund"
    ],
    "patterns": [
      "returnFunds limited to joint contributor workflow",
      "regular joinGroup members cannot recover deposit"
    ]
  },
  {
    "id": "storage-function-escrow-001",
    "source": "cyfrin/escrow.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SablierBobState",
    "functions_affected": [
      "isStakedInAdapter"
    ],
    "description": "`SablierBobState` 合约为 `Vault` 结构体的所有成员实现了 getter 函数，但未为布尔成员 `isStakedInAdapter` 实现 getter。该字段指示 vault 资金是否当前质押在适配器中，对链下集成和状态监控很重要。",
    "impact": "链下工具和集成无法直接查询 `isStakedInAdapter`，需要使用低级存储读取，降低了合约的可用性和可观测性。",
    "code_snippet": "function isStakedInAdapter(...) { ... }  // see audit report",
    "location": "SablierBobState.sol (isStakedInAdapter)",
    "recommendation": "为 `Vault` 结构体的 `isStakedInAdapter` 成员实现 getter 函数。",
    "tags": [
      "missing-struct-getter",
      "isStakedInAdapter",
      "vault-state-observability",
      "low-level-sload-required"
    ],
    "patterns": [
      "Vault getters for all fields except isStakedInAdapter",
      "integrations must read raw storage slot"
    ]
  },
  {
    "id": "storage-function-trade-001",
    "source": "cyfrin/trade.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "BasisTradeVault",
    "functions_affected": [
      "deposit"
    ],
    "description": "`BasisTradeVault::deposit` 未强制执行最低存款金额。允许粉尘存款可能导致经济不合理——用户存入的金额可能远小于交易的 gas 费用。此外还可能引发攻击者用大量微小存款轰炸 vault 的场景。",
    "impact": "用户可以提交经济上不合理的粉尘存款，交易成本远超存入价值；恶意或意外的粉尘存款增加 vault 管理开销。",
    "code_snippet": "// `BasisTradeVault::deposit` does not enforce a minimum deposit amount. Allowing dust deposits can lead to economic unviab",
    "location": "BasisTradeVault.sol (deposit)",
    "recommendation": "引入可由管理员配置的 `minDepositAmount` 状态变量，并修改 `deposit` 函数以要求存入的 `assets` 大于或等于该最小金额。",
    "tags": [
      "dust-deposit",
      "no-minimum-deposit",
      "vault-spam",
      "BasisTradeVault-deposit"
    ],
    "patterns": [
      "deposit accepts arbitrarily small amounts",
      "gas cost exceeds deposit value"
    ]
  },
  {
    "id": "storage-function-protocol-002",
    "source": "cyfrin/protocol.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SessionManager",
    "functions_affected": [
      "joinGame"
    ],
    "description": "`SessionManager::joinGame` 对 `games[_gameId]` 的 `state`（可能两次）、`numContestants` 和 `verificationRequired` 最多执行 4 次存储读取。通过重构 `Game` 结构体，将 `state`、`numContestants`（uint32）和 `verificationRequired`（bool）打包到同一存储槽，可以通过单次存储读取获取所有三个值。",
    "impact": "当前实现每次 `joinGame` 调用需要最多 4 次存储读取，更好的打包可将其减少为 1 次，在规模化时节省大量 gas。",
    "code_snippet": "// `SessionManager::joinGame` performs up to 4 storage reads to access `games[_gameId].state` (potentially twice), `games[_",
    "location": "SessionManager.sol (joinGame)",
    "recommendation": "重构 `Game` 结构体以将 `uint32 numContestants`、`SessionState state` 和 `bool verificationRequired` 打包到同一存储槽，在 `joinGame` 中通过存储引用一次性读取所有三个字段。",
    "tags": [
      "storage-slot-packing",
      "joinGame-multiple-sload",
      "Game-struct-layout",
      "gas-optimization"
    ],
    "patterns": [
      "up to 4 SLOADs for state/numContestants/verificationRequired",
      "fields could pack into single slot"
    ]
  },
  {
    "id": "storage-function-clob-001",
    "source": "cyfrin/clob.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "ConditionalTokens",
    "functions_affected": [
      "redeemVoided",
      "resolveEvent"
    ],
    "description": "独立二元市场通过 `adminVoidMarket` 支持取消，设置 `resolvedOutcome = -1` 并允许参与者通过 `ConditionalTokens::redeemVoided` 按比例取回抵押物。但负风险事件市场没有等效路径：`adminVoidMarket` 硬性拒绝负风险市场，而 `NegRiskAdapter::resolveEvent` 只接受获胜结果，不接受取消。如需取消负风险事件，管理员只能让其永久未解决（锁定所有参与者抵押物）或将其解析为\"其他获胜\"（将所有抵押物转入国库而非退还参与者），两者均不是公平的取消机制。",
    "impact": "当预言机不可用、问题无效或监管行动要求取消事件时，没有安全机制返还参与者的抵押物，资金将永久锁定或被错误地转入国库。",
    "code_snippet": "function redeemVoided(...) { ... }  // see audit report",
    "location": "ConditionalTokens.sol (redeemVoided, resolveEvent)",
    "recommendation": "在 `NegRiskAdapter` 中添加 `voidEvent` 函数，对每个底层市场调用 `adminVoidMarket`，设置 `evt.resolved = true`，并处理适配器的 wcol 核算，使参与者能够按比例取回抵押物。",
    "tags": [
      "neg-risk-void-missing",
      "market-cancellation-gap",
      "collateral-lock",
      "adminVoidMarket"
    ],
    "patterns": [
      "binary markets have redeemVoided",
      "neg-risk events cannot be fairly voided"
    ]
  },
  {
    "id": "storage-function-protocol-003",
    "source": "cyfrin/protocol.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SessionManager",
    "functions_affected": [
      "createGame",
      "_commitQuestions",
      "setXPTiers"
    ],
    "description": "多个外部函数对仅读取而不修改的数组参数使用了 `memory` 而非 `calldata`，包括 `SessionManager::createGame`、`QuestionManager::_commitQuestions` 和 `DefaultSession::setXPTiers` 中的相关参数。对只读外部输入使用 `memory` 会强制将 calldata 不必要地复制到内存，浪费 gas。",
    "impact": "每次调用受影响的函数都需为输入数组的内存分配和复制支付额外 gas，对大型数组影响尤为显著。",
    "code_snippet": "// Several external functions use `memory` for array parameters that are only read, not modified. Specifically, `SessionMan",
    "location": "SessionManager.sol (createGame, _commitQuestions, setXPTiers)",
    "recommendation": "将所有外部函数中只读数组参数的 `memory` 改为 `calldata`。",
    "tags": [
      "calldata-vs-memory",
      "external-array-copy-gas",
      "read-only-params",
      "createGame"
    ],
    "patterns": [
      "memory array params force calldata copy",
      "calldata should be used for read-only external inputs"
    ]
  },
  {
    "id": "storage-function-protocol-004",
    "source": "cyfrin/protocol.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SessionManager",
    "functions_affected": [
      "createGame",
      "joinGame",
      "_resolveSession"
    ],
    "description": "整个代码库中，`sessionId` 和 `gameId` 被互换使用来指代同一概念——游戏会话的标识符。这种不一致性跨越多个合约，使代码库难以阅读、理解和维护。",
    "impact": "命名不一致降低了代码可读性和可维护性，增加了集成或未来修改时开发者产生混淆的风险。",
    "code_snippet": "// sessionId and gameId used interchangeably for same identifier across contracts",
    "location": "Protocol contracts (SessionManager, DepositManager, etc.)",
    "recommendation": "选择一个名称（`sessionId` 或 `gameId`），并在所有合约、natspec、事件、错误和映射中一致使用。",
    "tags": [
      "naming-inconsistency",
      "sessionId-gameId",
      "maintainability",
      "developer-confusion"
    ],
    "patterns": [
      "same concept named sessionId in some contracts gameId in others",
      "cross-contract identifier mismatch"
    ]
  },
  {
    "id": "storage-function-VechainFoundation-002",
    "source": "HackenPDFTXT/Vechain Foundation.txt",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "StargateNFT",
    "functions_affected": [
      "migrateTokenManager"
    ],
    "description": "`TokenManager` 库中的 `migrateTokenManager` 函数包含一个存储不一致缺陷，在迁移操作期间会破坏合约状态。该函数直接设置新管理者而不从先前管理者的 `managerToTokenIds` 集合中移除代币，导致多个用户看似同时管理同一代币的异常状态。",
    "impact": "迁移数据损坏导致视图函数不一致：`getTokenManager()` 和 `isTokenManager()` 返回正确数据，但 `idsManagedBy()` 返回过时条目，带来额外 gas 开销且损坏状态难以修复。",
    "code_snippet": "function migrateTokenManager(...) {\n    $.managerToTokenIds[_manager].add(_tokenId);  // old manager set not cleaned\n    $.tokenIdToManager[_tokenId] = _manager;\n}",
    "location": "TokenManager library (migrateTokenManager)",
    "recommendation": "更新 `migrateTokenManager` 以包含适当的清理逻辑：在添加新管理者之前，从先前管理者的 `managerToTokenIds` 集合中移除代币并清除现有映射条目。",
    "tags": [
      "token-manager-migration",
      "stale-managerToTokenIds",
      "storage-inconsistency",
      "idsManagedBy-corruption"
    ],
    "patterns": [
      "new manager added without removing from previous manager set",
      "getTokenManager correct but idsManagedBy stale"
    ]
  },
  {
    "id": "storage-function-accountable-001",
    "source": "cyfrin/accountable.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "AccountableOpenTerm",
    "functions_affected": [
      "_calculateRequiredLiquidity",
      "_getAvailableLiquidityForProcessing",
      "supply",
      "repay"
    ],
    "description": "`AccountableOpenTerm` 和 `AccountableFixedTerm` 中的多个函数对相同存储变量执行了冗余的重复读取，可缓存到局部变量中。关键模式包括：`_calculateRequiredLiquidity` 在嵌套函数调用中多次读取 `vault` 和 `_scaleFactor`；`_getAvailableLiquidityForProcessing` 重复读取 `vault`；`_penaltyFee` 使用未缓存的 `gracePeriod`；`supply` 和 `repay` 重复读取 `vault`；`_sharePrice` 重复读取 `loanState` 等。",
    "impact": "冗余存储读取增加了受影响函数所有调用者的 gas 成本，在协议核心 vault 操作中积累的低效影响不可忽视。",
    "code_snippet": "address vault_ = vault;  // recommended: cache repeated storage reads\n// multiple functions read vault, _scaleFactor, loanState multiple times per call",
    "location": "AccountableOpenTerm.sol, AccountableFixedTerm.sol",
    "recommendation": "将重复存储读取缓存到局部变量（如 `address vault_ = vault`），在可行时将缓存值作为参数传递，并通过适当初始化消除冗余条件读取。",
    "tags": [
      "redundant-sload",
      "storage-read-caching",
      "gas-inefficiency",
      "vault-loan-state"
    ],
    "patterns": [
      "same storage variable read 2+ times per function",
      "nested calls re-read vault and loanState"
    ]
  },
  {
    "id": "storage-function-wannabetv2-001",
    "source": "cyfrin/wannabetv2.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "Bet",
    "functions_affected": [
      "cancel"
    ],
    "description": "当投注使用 Aave 池但从未变为 `ACTIVE`（接受方从未接受）时，只有庄家的质押被供应给 Aave。在 `Bet::cancel` 中，回收的 Aave 余额仍然使用庄家/接受方的退款逻辑进行分配，导致从未存款的接受方可能获得庄家积累的 Aave 收益的一部分。",
    "impact": "从未接受投注的接受方在取消时可能获得庄家积累收益的一部分，代表对庄家资金的意外转移。",
    "code_snippet": "// When a bet uses an Aave pool but never becomes `ACTIVE` (the taker never accepts), only the maker's stake is supplied to",
    "location": "Bet.sol (cancel)",
    "recommendation": "仅向实际存入资金的一方分配 Aave 收益。若接受方从未接受投注，任何产生的收益应归庄家或国库所有，而非接受方。",
    "tags": [
      "aave-yield-misallocation",
      "cancel-before-accept",
      "taker-unearned-yield",
      "bet-cancel-refund"
    ],
    "patterns": [
      "only maker stake supplied when bet never ACTIVE",
      "cancel splits Aave yield to taker who never deposited"
    ]
  },
  {
    "id": "storage-function-protocol-005",
    "source": "cyfrin/protocol.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SessionManager",
    "functions_affected": [
      "Game",
      "startTime",
      "SessionResult",
      "time"
    ],
    "description": "协议中多个与时间戳相关的字段被声明为 `uint256`，而 `uint32` 已足够（`uint32` 最大值对应 2106 年，远超协议预期生命周期）。将 `startTime`、`endTime`、`originalStartTime` 及各时间相关字段改为 `uint32` 可允许相邻时间戳字段共享存储槽，显著减少所需存储槽数量。",
    "impact": "使用 `uint256` 存储时间戳浪费存储槽，增加所有游戏生命周期函数的 SLOAD/SSTORE 成本。",
    "code_snippet": "function Game(...) { ... }  // see audit report",
    "location": "SessionManager.sol (Game, startTime, SessionResult, time)",
    "recommendation": "将所有时间戳和持续时间字段声明为 `uint32`，并在各自结构体中将其与其他小类型字段相邻排列以实现存储打包。",
    "tags": [
      "uint256-timestamp-waste",
      "storage-slot-packing",
      "Game-struct",
      "uint32-sufficient"
    ],
    "patterns": [
      "startTime/endTime as uint256 each own slot",
      "uint32 timestamps sufficient until 2106"
    ]
  },
  {
    "id": "storage-function-vesting-001",
    "source": "cyfrin/vesting.md",
    "category": "Upgrade / Config",
    "severity": "Medium",
    "severity_level": 2,
    "contract_name": "SDLVesting",
    "functions_affected": [
      "constructor"
    ],
    "description": "`SDLVesting` 将 `uint256[] private reSDLTokenIds` 声明为动态数组，并在构造函数中通过循环将其初始化为 `MAX_LOCK_TIME + 1`（5个）元素。这会产生动态数组长度槽、数据指针槽和多次存储写入。由于数组长度始终恰好为 `MAX_LOCK_TIME + 1`，静态数组 `uint256[MAX_LOCK_TIME + 1]` 完全等效，可消除动态数组开销并省去初始化循环。",
    "impact": "使用动态数组而非固定长度数组浪费两个存储槽（长度 + 数据指针），在部署时产生不必要的存储写入，整个合约生命周期内每次读取的 gas 成本也略高。",
    "code_snippet": "uint256[] private reSDLTokenIds;  // dynamic array with fixed length MAX_LOCK_TIME+1\n// should be: uint256[MAX_LOCK_TIME + 1] private reSDLTokenIds;",
    "location": "SDLVesting.sol (reSDLTokenIds)",
    "recommendation": "将动态数组声明替换为 `uint256[MAX_LOCK_TIME + 1] private reSDLTokenIds`，并移除构造函数初始化循环。",
    "tags": [
      "dynamic-vs-fixed-array",
      "storage-overhead",
      "deployment-gas",
      "vesting-token-ids"
    ],
    "patterns": [
      "fixed-size data in dynamic array",
      "extra length slot and init loop vs static array"
    ]
  }
]
